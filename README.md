### Guelph Innovation District Engagement Web app     

React web app built by Sasaki.    

#### Suggestion for dev/build    
install libraries with `pnpm install` then run with `pnpm start` or build with `pnpm build`